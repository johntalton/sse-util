{
  "name": "@johntalton/sse-util",
  "version": "0.0.4",
  "main": "./lib/sse.js",
  "author": "johntalton@gmail.com",
  "license": "MIT",
  "exports": {
    "import": "./lib/sse.js"
  },
  "type": "module",
  "files": [
    "src/**/*.ts",
    "lib/**/*.js",
    "lib/**/*.d.ts",
    "lib/**/*.d.ts.map",
    "lib/**/*.js.map"
  ],
  "types": "lib/sse.d.ts",
  "scripts": {
    "build": "tsc -p .",
    "build:watch": "npm run build -- -w",
    "test": "node --test",
    "coverage": "c8 --check-coverage --all --reporter lcov --reporter text npm --silent run test"
  },
  "devDependencies": {
    "c8": "^10.1.3",
    "typescript": "^5.9.2"
  }
}
